#ifndef DISK_SCHEDULER_H
#define DISK_SCHEDULER_H

class DiskScheduler {
    /*
    handles flush async
    queue for tracking dirty pages
    once reaches x threshold flush y number of pages
    write to disk to flush
    */
};

#endif